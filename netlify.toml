# Netlify configuration — castorene.cl
# GitHub Pages es el hosting activo. Estas cabeceras solo se aplicarán si el
# sitio vuelve a desplegarse desde Netlify; no sustituyen controles en GitHub.
[build]
  publish = "."

# Force the canonical host to www.castorene.cl (redirect apex -> www)
[[redirects]]
  from = "https://castorene.cl/*"
  to = "https://www.castorene.cl/:splat"
  status = 301
  force = true

# Security & caching headers
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "geolocation=(), microphone=(), camera=()"
